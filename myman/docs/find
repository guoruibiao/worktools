基本格式：find  path expression
示例：
  1.查找 某个路径（及其子路径）下 文件名为XXX
      find /targetpath -name XXX
  2.查找 某个路径（及其子路径）下 文件所属人 
      find /targetpath -user XXX
  3.注意
      find / 路径会巨耗CPU，高老板建议禁止🚫这么用。
  4. 模糊查找
      find . -iname '*log' -type f | xargs ls -lhaSr
      
