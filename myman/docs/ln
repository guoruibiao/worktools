1.功能:
    创建软链接,为了保证两个文件(目录ps要用绝对路径)的*同步性*,在不同的目录下用到相同的文件,不需要复制过来,只需要创建一个链接,减少了磁盘的占用.

2.示例:
    (软)  ln -s 111.txt(源) 222.txt(目的)    222.txt -> 111.txt
    (硬)  ln 111.txt 222.txt

3.备忘:软硬连接的区别:
    1.软链接可以跨越文件系统,硬链接不行
    2.软链接可以对目录创建,硬链接不行
    3.软链接以路径的形式存在,硬链接表现为副本,所以删除源后,软链接可以重新连上,硬链接就彻底断了
